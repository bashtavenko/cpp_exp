{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Bazel launch",
      "type": "cppdbg",
      "request": "launch",
      "preLaunchTask": "Bazel build",
      "cwd": "${workspaceFolder}/bazel-out/k8-dbg/bin/hello_main.runfiles",
      "program": "${workspaceFolder}/bazel-out/k8-dbg/bin/hello_main",
      "args": ["--logtostderr"]
    },
  ]
}
